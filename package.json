{
  "name": "travel-agent",
  "private": true,
  "packageManager": "pnpm@9.15.5",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:integration": "turbo run test:integration",
    "test:e2e": "turbo run test:e2e",
    "db:migrate": "pnpm --filter @travel/persistence db:migrate",
    "security:scan-sensitive-output": "rg -n \"(id_card|身份证|银行卡|password|secret)\" apps packages --glob '!**/*.test.*' --glob '!**/*.spec.*' || exit 0",
    "ci": "pnpm typecheck && pnpm lint && pnpm test && pnpm test:integration && pnpm test:e2e && pnpm security:scan-sensitive-output"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^8.29.1",
    "turbo": "2.10.11",
    "typescript": "^5.8.3",
    "eslint": "^9.24.0"
  }
}
