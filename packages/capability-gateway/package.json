{
  "name": "@travel/capability-gateway",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "pretest": "pnpm --filter @travel/contracts build",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --no-warn-ignored",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": { "@travel/contracts": "workspace:*", "zod": "^3.25.76" },
  "devDependencies": { "typescript": "^5.8.3", "eslint": "^9.24.0", "vitest": "^3.1.1" }
}
