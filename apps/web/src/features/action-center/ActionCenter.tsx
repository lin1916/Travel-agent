export function ActionCenter({ actions = [] }: { actions?: Array<{ id: string; label: string; blocked?: string }> }) { return <section className="section-block" aria-labelledby="action-center-title"><h2 id="action-center-title">行动中心</h2>{actions.length === 0 ? <p className="muted">没有待处理操作</p> : <ul className="plain-list">{actions.map(action => <li key={action.id}>{action.label}{action.blocked ? `（${action.blocked}）` : ''}</li>)}</ul>}</section>; }
