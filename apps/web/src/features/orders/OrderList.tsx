import type { ReactNode } from 'react';
export interface OrderListItem { id: string; supplierId: string; lifecycleStatus: string; reconciliationStatus: string; requiredUserAction?: string }
export function OrderList({ orders, renderDetail }: { orders: OrderListItem[]; renderDetail?: (order: OrderListItem) => ReactNode }) { return <section className="section-block" aria-labelledby="orders-title"><div className="section-heading"><h2 id="orders-title">订单</h2><span className="muted">供应商状态实时更新</span></div>{orders.length === 0 ? <p className="muted">暂无订单</p> : <ul className="plain-list">{orders.map(order => <li key={order.id}><strong>{order.supplierId}</strong> · {order.lifecycleStatus} · {order.reconciliationStatus}{order.requiredUserAction ? ` · ${order.requiredUserAction}` : ''}{renderDetail?.(order)}</li>)}</ul>}</section>; }
